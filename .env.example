# Required API Keys
TRAKT_CLIENT_ID=
TRAKT_CLIENT_SECRET=
TMDB_API_KEY=
RECOMBEE_DATABASE_ID=
RECOMBEE_PRIVATE_TOKEN=
# Recombee region: us-west | eu-west | ap-se
RECOMBEE_REGION=us-west

# Security
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=
# Random 32-byte hex for session signing
SECRET_KEY=
# Token for /admin/* endpoints (manual sync triggers, status). Leave blank to disable admin.
ADMIN_TOKEN=

# App
BASE_URL=https://reclio.p0xl.com
PORT=8000

# Ollama
OLLAMA_BASE_URL=http://ollama:11434
OLLAMA_MODEL=llama3.2:3b

# Sync intervals (hours)
USER_SYNC_INTERVAL_HOURS=4
CONTENT_SYNC_INTERVAL_HOURS=24
TOKEN_REFRESH_INTERVAL_HOURS=6

# Database
DATABASE_URL=sqlite+aiosqlite:///./data/db/reclio.db

# ChromaDB
CHROMA_PERSIST_DIR=./data/chroma
